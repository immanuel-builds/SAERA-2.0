# Finding State Notes

NetVuln stores simple status information for findings so users can mark issues as open, investigating, mitigated, resolved, or suppressed.

This is a helper feature. The main project remains:

```text
scan target -> save results -> review findings -> show scan detail
```

More advanced state tracking can be described as future scope.